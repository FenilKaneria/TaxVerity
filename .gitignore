# Corpus source document — 101.8 MiB, over GitHub's 100 MiB per-file limit.
# Reproducibility is via the content hash recorded in the corpus manifest
# (Step 1.2), not via version control of the blob itself.
Income-tax-Act-2025.pdf

# Assistant config and the local planning documents it maintains.
# Untracked by explicit project decision: the repo carries code, not the
# planning spine. Both are still the working source of truth locally.
.claude/
PLAN.md
DECISIONS.md

# Derived corpus artifacts — regenerable from the PDF via scripts/ and keyed
# by corpus_version (ADR-005). Step 1.2 onward writes large span-level JSONL
# here. reports/ is deliberately NOT ignored: it is the evidence.
data/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/

# Tooling caches
.pytest_cache/
.ruff_cache/
.hypothesis/

# Environment
.env

# OS
Thumbs.db
.DS_Store
