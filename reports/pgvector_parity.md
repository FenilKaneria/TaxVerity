# pgvector parity — Income-tax Act, 2025

Auto-generated by `scripts/measure_parity.py` (Step 6.5). The Postgres
index against the NumPy one on the gold v2 set, searched with the query
vectors embedded once at Step 5.2 — this run bills no tokens.

| Measure | Value |
|---|---|
| corpus_version | `3bc9b522e13d4b93…` |
| Chunks | 8351 |
| Gold queries | 80 |
| Primary k | 10 |
| Model | `jina-embeddings-v5-text-small@hosted-unpinned dim=1024 runtime=jina-api encoding=jina-api/task+normalized/v1` |
| Embedding set | 1 (8351 vectors) |
| HNSW | m=16, ef_construction=64, ef_search=40 |
| HNSW build | 1.9s, 65 MB |
| Run time | 37.3s |

## Search latency, k=20

Best of three per query, one query at a time, from this machine against
the local container. The chunk join is included: it is what serving
returns.

| index | plan | p50 | p95 |
|---|---|---|---|
| numpy | `in-process` | 1.77 ms | 1.92 ms |
| pgvector exact | `Seq Scan on chunk_embeddings e` | 48.55 ms | 51.21 ms |
| pgvector HNSW | `Index Scan using chunk_embeddings_hnsw on chunk_embeddings e` | 3.41 ms | 4.66 ms |
| pgvector HNSW, rebuilt | `Index Scan using chunk_embeddings_hnsw on chunk_embeddings e` | 3.36 ms | 3.97 ms |

## Ranking agreement against NumPy, top 20

By chunk id. **Reordered** is the same chunks in a different order — for
an exact index that is a broken tie-break, not noise.

| index | identical | reordered | different | mean overlap | max score delta |
|---|---|---|---|---|---|
| pgvector exact | 80/80 | 0 | 0 | 1.000 | 1.45e-07 |
| pgvector HNSW | 62/80 | 0 | 18 | 0.960 | 1.45e-07 |

The last row compares the two HNSW builds with each other, not with
NumPy: the same vectors, the same settings, a second randomised graph.

| comparison | identical | reordered | different | mean overlap | max score delta |
|---|---|---|---|---|---|
| HNSW build 1 vs build 2 | 72/80 | 0 | 8 | 0.974 | 0.00e+00 |

## Gold-set scores

Every score twice (ADR-060): **strict** credits only the labelled unit,
**lenient** also credits an ancestor.

| retriever | strict R@10 | lenient R@10 | lenient MRR | lenient nDCG@10 | lenient R@20 |
|---|---|---|---|---|---|
| numpy — dense | 0.688 | 0.727 | 0.570 | 0.576 | 0.828 |
| pgvector exact — dense | 0.688 | 0.727 | 0.570 | 0.576 | 0.828 |
| pgvector HNSW — dense | 0.680 | 0.719 | 0.563 | 0.572 | 0.820 |
| pgvector HNSW, rebuilt — dense | 0.688 | 0.727 | 0.578 | 0.582 | 0.828 |
| numpy — hybrid + shortcut | 0.648 | 0.797 | 0.717 | 0.700 | 0.836 |
| pgvector exact — hybrid + shortcut | 0.648 | 0.797 | 0.717 | 0.700 | 0.836 |
| pgvector HNSW — hybrid + shortcut | 0.664 | 0.797 | 0.717 | 0.700 | 0.820 |
| pgvector HNSW, rebuilt — hybrid + shortcut | 0.664 | 0.805 | 0.718 | 0.703 | 0.828 |

Per slice, lenient recall / lenient nDCG at k=10, dense only:

| slice | numpy | pgvector exact | pgvector HNSW | pgvector HNSW, rebuilt |
|---|---|---|---|---|
| citation | 0.900 / 0.691 | 0.900 / 0.691 | 0.900 / 0.691 | 0.900 / 0.691 |
| paraphrase | 0.714 / 0.572 | 0.714 / 0.572 | 0.714 / 0.585 | 0.714 / 0.585 |
| crossref | 0.531 / 0.440 | 0.531 / 0.440 | 0.500 / 0.401 | 0.531 / 0.440 |

## Verdict — parity

**Identical.** Two exact searches
of the same vectors must return the same ranking; a score difference up to
1e-05 is float noise.

The Postgres index is a drop-in for the NumPy one on this corpus.

## Verdict — HNSW

Registered in `evals/parity.py` before this run: an approximate index is
adopted only if exact search p95 exceeds **100 ms**
— the budget argued from the query path's own vendor round trips, query
embedding at p50 324 ms (Step 4.6) and reranking at p95 1,126 ms (Step
5.6) — **and** costs no lenient recall@10 or nDCG@10.

**REJECTED.**

- exact search p95 51.2 ms is within the 100 ms budget, so there is no latency constraint to relieve
- lenient recall fell 0.727 -> 0.719
- lenient nDCG fell 0.576 -> 0.572

A third cost the rule does not price, measured above: two builds of the
same index over the same vectors do not rank alike, so an approximate
index would make every retrieval measurement in this project depend on
which graph happened to be built.

Both indexes were dropped after the measurement. The numbers above are
what HNSW would cost and buy if Step 6.6, or a larger corpus, ever needs
it.
